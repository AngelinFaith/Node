// You can export the data using module.exports and import it in the file where we can use it.
// OR without exporting , you can just call require() in the to import the whole file and the action performed here
// is displayed in node.
let subject= "Node";
let isAdmin = true;

console.log(isAdmin);

// module.exports={subject, isAdmin};